# Firebase REST config for the waitlist.
# Use this when service account key creation is blocked by organisation policy.

FIREBASE_PROJECT_ID=tumble-waitlist
FIREBASE_WEB_API_KEY=your-firebase-web-api-key
